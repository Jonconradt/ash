version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - errcheck
    - gocritic
    - nilerr
    - noctx
    - perfsprint

  settings:
    perfsprint:
      strconcat: false

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
